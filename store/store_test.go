package store

import "testing"

func TestGet(t *testing.T) {
	/* // Given
	store := NewStore()

	// When
	got, err := store.Get()
	if err != nil {
		t.Errorf("err: %v\n", err)
	}

	// Then
	expected := Response{}
	if got != expected {
		t.Errorf("got: %s, want: %s", got, expected)
	} */
}
